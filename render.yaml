services:
  - type: web
    name: bonostyle-qr-generator
    env: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
      - key: NPM_VERSION  
        value: 9.6.7
